# 2026-09-07 发布准备

本次仅整理代码、验证和生成本地发布包。未连接生产服务器、上传文件、启动翻译、执行迁移或重启服务。

## 代码范围

准备分支为 `codex/current-latest`。准备时逐一检查全部本地分支，相对当前 HEAD 均无待合并提交。`main` / `codex/primary-agent` 的 `790cad3` 也已包含在本分支中，不需要重复合并。所有当前应用代码改动纳入版本；不纳入根目录客户导入文件、工作簿、私密配置或运行时数据。

相对 `790cad3` 包括：

- 自动增量翻译：独立 worker 监控商品变更、合并短时间连续修改，共用配置中心并发与重试；后台可看到自动任务状态，避免与手动操作冲突。默认关闭，由管理员按商家/语言开启。
- 恢复商品、批量商品、一级/二级分类分享；为公开分享链接返回服务端 OG / Twitter 卡片信息。沿用当前账号店铺身份，不泄露供应商、成本或主账号资料。
- 子账号独立前台地址与报价归属修复；有装箱数的 SKU 按整箱采购，未设置时不显示空装箱数。
- 前台目录查询、缓存和预取优化；热门商品优先展示设置移到商品列表；询价页按钮补译、缩短英文状态文案。
- 补齐 8 种扩展语言中的热门商品文案和分享入口占位译文。
- 发布预检修复：各 Compose 迁移版本统一为 `20260906_0134`，迁移测试读取实际 head。新触发器的必要授权与迁移同事务生效，完整数据库授权也改为原子提交，避免在线请求观察到临时撤权状态。

## 验证

- 完整 `npm run build` 通过：8 种扩展语言各 3754 / 3754 项通过，无占位或语言错误；TypeScript 与 Vite 生产构建通过。
- 前端全部 15 组 `scripts/test-*.mjs` 通过，包含分享真实 Vite 代理检查、装箱下单、子账号路由、单证、目录性能与自动翻译。
- 后端功能、翻译、隔离、分享、采购、架构与部署配置回归：184 项通过。
- 扩展 API 回归：275 项通过，发现迁移测试仍写死旧 head；修正后该测试单独复测通过（覆盖 SQLite 完整升级、降级及模型一致性检查）。合计覆盖通过 276 项 API 测试。
- `git diff --check` 及部署/回滚脚本 `bash -n` 通过。

已知限制与待验收：

- `test_catalog_translation_job_reports_progress_and_caches_results` 未纳入本轮扩展 API 回归。此前在修改前 HEAD 的独立副本也复现：报价草稿切回中文时保留英文名称快照。本次未处理这个已有问题，不能声称完整 API 套件全绿。
- 浏览器连接超时，分享入口的实际视觉验收尚未完成；不能以构建和源码测试代替浏览器验收。
- Vite 保留现有大 chunk 警告；不阻止打包。
- 本机无 Docker CLI；未构建或启动 Linux 容器，也未在生产 PostgreSQL 执行迁移。PostgreSQL 迁移 SQL / RLS 及授权事务通过本地结构与模拟检查。

## 后续上线顺序

1. 先核对线上当前提交、迁移 head、容器拓扑和运行任务，保留现有生产配置。发布包中的增量 Git bundle 以 `790cad3` 为前置提交；线上若不具备该提交，先补齐历史，不能盲目套用。
2. 在旧 API / Web 正常运行时导入固定版本、构建 Linux 镜像、做配置检查和现有在线备份。不要直接运行上次 `790cad3` 的 app-only 脚本：本次有新迁移和新 worker。
3. 执行迁移至 `20260906_0134` 及运行时授权。保留租户 RLS，不批量重翻译历史数据，不移动或备份云端 R2 图片。
4. 镜像与迁移准备好后再短时切换 API / Web，健康检查通过后更新独立 `translation-worker`。不提前停站等待构建，不让翻译任务影响网站健康检查。
5. 核对健康接口版本、子账号下单归属、整箱数量、商品/分类分享 HTML 元数据和自动翻译开关。记录实际切换耗时；如失败，用保存的上一个应用版本回滚，不盲目回退数据库。

源代码包、增量 bundle、SHA-256 和确切提交号见本地 `outputs/releases/` 中本次准备目录。生产镜像须使用服务器现有生产参数构建；本地构建只作为发布预检，不替代线上镜像。
